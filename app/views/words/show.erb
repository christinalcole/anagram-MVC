<h1>Database entry for <%= "#{@word.text}" %></h1>
<table class="table">
  <thead>
    <th>ID</th>
    <th>Text</th>
    <th>Letters</th>
  </thead>
  <tbody>
    <tr>
      <td><%= "#{@word.id}" %></td>
      <td><%= "#{@word.text}" %></td>
      <td><%= "#{@word.letters}" %></td>
    </tr>
  </tbody>
</table>
